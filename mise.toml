[tools]
"http:pencil" = { version = "4.0.0", url = "https://www.pencil.dev/download/Pencil-linux-x64.tar.gz" }
node = "25.9.0"

[tasks.install]
run = "npm install"

[tasks.dev]
run = "npm run storybook"
[tasks.test]
run = "npm run test"
